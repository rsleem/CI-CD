apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: server
  namespace: server
spec:
  rules:
  - host: server-dev.fake
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: server
            port: 
              number: 80 