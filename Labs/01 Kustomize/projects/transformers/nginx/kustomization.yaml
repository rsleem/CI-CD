apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../base-transformers

images:
- name: replaceme
  newName: nginx

namePrefix: nginx-

commonLabels:
    app: nginx
    run: nginx
    namespace: nginx
    
patchesStrategicMerge:
- patch.yaml